type AudioSettings = {
    track: string;
    volume: number;
    lastChange: number;
}

type EffectSettings = {
    id: string,
    name: string
}

type ScreenSettings = {
    screenType: string,
    config: any
}
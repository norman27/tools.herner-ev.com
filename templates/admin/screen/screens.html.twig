{% extends 'admin/base.html.twig' %}

{% block body_content %}
    <div class="row">
        <div class="col">
            <div class="card">
                <div class="card-header">
                    <i class="far fa-list-alt"></i>Screens</div>
                <div class="card-body" id="screen-list">{# React #}</div>
            </div>
        </div>
    </div>
{% endblock %}